After installing tensorflow 2.6:

pip install tensorflow-hub
pip install tensorflow-estimator==2.6.0
conda install git
pip install git+https://github.com/tensorflow/docs
pip install wget
pip install pyttsx3
pip install pipwin
pipwin install pyaudio
pip install SpeechRecognition
pip install pandas==1.2.5 (if needed)
pip install numpy==1.7.0 (if needed)